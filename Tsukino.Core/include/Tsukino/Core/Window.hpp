//--------------------------------------------------------------
//! @file	Window.hpp
//! @brief  ウィンドウ管理クラスの宣言
//! @author 山﨑愛
//--------------------------------------------------------------
#pragma once
#include <windows.h>
#include <string>
#include <functional>
// 名前空間 : Tsukino::Core
namespace Tsukino::Core {
    //--------------------------------------------------------------
    //! @class	 Window
    //! @brief	 ウィンドウ管理クラス
    //! @details Win32 APIを使用してウィンドウの生成とメッセージ処理を行うクラス
    //--------------------------------------------------------------
    class Window {
    public:
        //--------------------------------------------------------------
        //! @brief ウィンドウスタイルの列挙型
        //--------------------------------------------------------------
        enum class WindowStyle {
            Default,        // 通常のゲームウィンドウ
            Popup,          // 枠なしウィンドウ
            ClickThrough    // 枠なし ＋ 背景透過 ＋ クリック透過
        };

        //--------------------------------------------------------------
        //! @brief ウィンドウ更新モードの列挙型
        //--------------------------------------------------------------
        enum class UpdateMode {
            ActiveOnly,       // 通常：フォーカス時のみ（省電力）
            AlwaysResident    // 常駐：バックグラウンドでも更新を維持
        };

    public:
        //--------------------------------------------------------------
        // メッセージコールバックの型エイリアス
        //--------------------------------------------------------------
        using MessageCallback = std::function<void(UINT, WPARAM, LPARAM)>;

        //--------------------------------------------------------------
        // リサイズ通知コールバックの型エイリアス
        //! @note 引数は新しいクライアント領域の幅・高さ（ピクセル）
        //--------------------------------------------------------------
        using ResizeCallback = std::function<void(int, int)>;

        //--------------------------------------------------------------
        // フォーカス喪失通知コールバックの型エイリアス
        //--------------------------------------------------------------
        using FocusLostCallback = std::function<void()>;

        //--------------------------------------------------------------
        // コンストラクタ
        //--------------------------------------------------------------
        Window();

        //--------------------------------------------------------------
        // デストラクタ
        //--------------------------------------------------------------
        ~Window();

        //--------------------------------------------------------------
        // ウィンドウ生成
        //! @param title  [in] ウィンドウタイトル
        //! @param width  [in] ウィンドウ幅
        //! @param height [in] ウィンドウ高さ
        //! @return ウィンドウの生成に成功した場合は true、失敗した場合は false
        //--------------------------------------------------------------
        [[nodiscard]]
        bool Create(const std::string& title, int width, int height, WindowStyle style = WindowStyle::Default);

        //--------------------------------------------------------------
        // メッセージ処理（false が返ればアプリ終了）
        //! @return true: 継続, false: 終了
        //--------------------------------------------------------------
        [[nodiscard]]
        bool ProcessMessages();

        //--------------------------------------------------------------
        // メッセージコールバックを設定する関数
        //! @param callback [in] メッセージコールバック関数
        //--------------------------------------------------------------
        void SetMessageCallback(MessageCallback callback) { m_callback = callback; }

        //--------------------------------------------------------------
        // リサイズ通知コールバックを設定する関数
        //! @param callback [in] クライアント領域のサイズが変わったときに呼ばれる関数
        //! @note  スワップチェインの再構築に使う。設定しないとウィンドウを
        //!        リサイズしても描画解像度が追従せず、映像が引き伸ばされる。
        //--------------------------------------------------------------
        void SetResizeCallback(ResizeCallback callback) { m_resizeCallback = std::move(callback); }

        //--------------------------------------------------------------
        // フォーカス喪失通知コールバックを設定する関数
        //! @param callback [in] ウィンドウが入力フォーカスを失ったときに呼ばれる関数
        //! @note  キーの押下状態をクリアするために使う。
        //!        WM_KEYUP はフォーカス移動先のウィンドウへ届くため、
        //!        これを行わないと Alt+Tab でキーが押しっぱなしのまま残る。
        //--------------------------------------------------------------
        void SetFocusLostCallback(FocusLostCallback callback) { m_focusLostCallback = std::move(callback); }

        //--------------------------------------------------------------
        // Rendererが必要とするHWND
        //! @return ウィンドウのハンドル
        //--------------------------------------------------------------
        [[nodiscard]]
        HWND GetHWND() const {
            return m_hWnd;
        }

        //--------------------------------------------------------------
        // ウィンドウの幅を取得する関数
        //! @return ウィンドウの幅
        //--------------------------------------------------------------
        [[nodiscard]]
        int GetWidth() const {
            if(m_isFullscreen) {
                return GetSystemMetrics(SM_CXSCREEN);
            }
            return m_width;
        }

        //--------------------------------------------------------------
        // ウィンドウの高さを取得する関数
        //! @return ウィンドウの高さ
        //--------------------------------------------------------------
        [[nodiscard]]
        int GetHeight() const {
            if(m_isFullscreen) {
                return GetSystemMetrics(SM_CYSCREEN);
            }
            return m_height;
        }

        //--------------------------------------------------------------
        // 動的に最前面表示を切り替える関数
        //! @param enable [in] true: 最前面表示, false: 通常表示
        //--------------------------------------------------------------
        void SetTopmost(bool enable);

        //--------------------------------------------------------------
        // 現在最前面かどうかを取得する関数
        //! @return true: 最前面表示, false: 通常表示
        //--------------------------------------------------------------
        [[nodiscard]]
        bool IsTopmost() const {
            return m_isTopmost;
        }

        //--------------------------------------------------------------
        //! @brief ウィンドウ更新モードを設定する関数
        //! @param mode [in] ウィンドウ更新モード
        //--------------------------------------------------------------
        void SetUpdateMode(UpdateMode mode);

        //--------------------------------------------------------------
        //! @brief フックからコールバックを呼び出すための公開メソッド
        //! @param msg    [in] メッセージコード
        //! @param wParam [in] メッセージの追加情報（通常は
        //! @param lParam [in] メッセージの追加情報（通常はイベントの座標など）
        //--------------------------------------------------------------
        void InvokeCallback(UINT msg, WPARAM wParam, LPARAM lParam);

        //--------------------------------------------------------------
        //! @brief 全画面表示の切り替え
        //! @param enable [in] true: 全画面表示, false: ウィンドウ表示
        //--------------------------------------------------------------
        void SetFullscreen(bool enable);

        //--------------------------------------------------------------
        //! @brief OSカーソルの表示/非表示を切り替える関数
        //! @details ShowCursorは内部で参照カウント方式のため、状態が
        //!          変化した時のみ呼び出すことで多重カウントを防いでいる
        //! @param visible [in] true: 表示, false: 非表示
        //--------------------------------------------------------------
        void SetCursorVisible(bool visible);

        //--------------------------------------------------------------
        //! @brief ウィンドウがフォアグラウンド（フォーカスされている）かを取得する関数
        //! @return true: フォーカスされている, false: されていない
        //--------------------------------------------------------------
        [[nodiscard]]
        bool IsFocused() const;

        //--------------------------------------------------------------
        //! @brief OSカーソルをクライアント領域の中央へ移動する関数
        //! @details マウスルック（無限に回転できるカメラ操作）を実現するために、
        //!          毎フレーム呼び出してカーソルを中央へ戻す用途を想定している
        //--------------------------------------------------------------
        void CenterCursor();

    private:
        //--------------------------------------------------------------
        // Win32 のウィンドウプロシージャ
        //! @param hwnd   [in]ウィンドウハンドル
        //! @param msg    [in]メッセージコード
        //! @param wParam [in] メッセージの追加情報（通常はイベントの種類など）
        //! @param lParam [in] メッセージの追加情報（通常はイベントの座標など）
        //! @return メッセージ処理の結果（通常は0）
        //--------------------------------------------------------------
        [[nodiscard]]
        static LRESULT CALLBACK WindowProc(HWND hwnd, UINT msg, WPARAM wParam, LPARAM lParam);

        //--------------------------------------------------------------
        //! @brief クリックスルーの場合に、フックを有効化する関数
        //--------------------------------------------------------------
        void EnableHooksIfClickThrough();

        //--------------------------------------------------------------
        //! @brief フックの状態を更新する関数
        //! @param shouldInstall [in] true: フックをインストール, false: フックをアンインストール
        //--------------------------------------------------------------
        void UpdateHookState(bool shouldInstall);

        HWND m_hWnd;      // ウィンドウハンドル
        int  m_width;     // ウィンドウの幅
        int  m_height;    // ウィンドウの高さ

        bool m_isTopmost = false;    // ウィンドウが常に最前面にあるかどうかのフラグ

        WindowStyle m_style;    // これで現在のスタイルを記憶しておく

        UpdateMode m_updateMode = UpdateMode::ActiveOnly;    // デフォルトは通常

        MessageCallback   m_callback;             // 連絡先を保存しておく変数
        ResizeCallback    m_resizeCallback;       // クライアント領域のサイズ変更通知先
        FocusLostCallback m_focusLostCallback;    // フォーカス喪失通知先

        RECT m_preFullscreenRect;    // フルスクリーン前のサイズを保持
        bool m_isFullscreen = false;

        bool m_cursorVisible = true;    // OSカーソルが現在表示されているか（ShowCursorの多重呼び出し防止用）
    };
}    // namespace Tsukino::Core
