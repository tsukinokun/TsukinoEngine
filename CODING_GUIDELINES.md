# CODING_GUIDELINES

**TsukinoEngine**で開発を行う際の、共通のコーディング規約を定めます。
ここに書かれていない事項は、**一貫性**と**可読性**を最優先し、既存コードに合わせます。

## 1. 対象と前提
- **対象言語:** ``C++20``（エンジン各モジュールの `cppdialect`。同梱の DirectXTex と
  JoltPhysics のみ ``C++17``）、``Premake`` の``ビルドスクリプト``、``シェーダ``、``スクリプト類``
- **対象範囲:**  
	- **エンジン本体コード**
	- ``サンプル`` / ``テストコード``

## 2. ファイル構成とインクルード

### 2-1. ヘッダとソース

- 原則**1クラスにつき1ヘッダ / 1ソース**とする(小さなユーティリティなどは例外とする)
- ヘッダには**インターフェースと最小限度の依存**だけを記述する
- **値を返すだけの関数や、テンプレート関数**を除き、実装詳細は可能な限り``.cpp``に出す

### 2-2. include の順序
1. **対応するヘッダ**（`Foo.cpp` なら `Foo.h`）
2. **同一モジュール内のヘッダ**
3. **他モジュールのヘッダ**
4. **標準ライブラリ / 外部ライブラリ**

- 序期の順序は**依存方向が上から下へ流れる**ようにすることで、循環依存や不要な再コンパイルを防ぐため
- 可能な限り **前方宣言**を使い、ヘッダの依存を減らす
- インクルードガードには `#pragma once` を使用する
- ``pch.``は``.cpp``にのみ適用されるため、ヘッダは``pch.h``に依存せず自己完結に書くこと。
- - 本プロジェクトでは、``pch.h``のみを``強制インクルード``している
- ``強制インクルード``は``include``の順序よりも前に自動挿入されるため、上記の順序ルールとは衝突しない

### 3. コーディングスタイル

- 本プロジェクトのコードスタイルは**clang-formatによって自動整形される**
- ``インデント``は**4スペース**（``.clang-format`` の ``UseTab: Never`` / ``IndentWidth: 4``、``BasedOnStyle: Google``）
- ``.clang-format``が唯一の正しいスタイル定義であり、手動整形は行わない
- ``UTF-8`` / ``CRLF``。コメントは日本語で書く

# 4.命名規則

### 4-1.共通方針
- 英語で命名する
- 略語は一般的なもの（``URL``, ``ID``, ``CPU``等）のみに限定する
- 名前から役割とスコープが推測できること

### 4-2.C++の命名

- **クラス**/**構造体** : ``PascalCase``
```cpp
class TextureLoader;
struct VertexBufferDesc;
```

- **関数**/**メソッド** : ``PascalCase ``
	- ただし**STL互換の関数**や、**ファイル名取得などの軽量アクセサ**は``camelCase``を許容
```cpp
int GetWidth();
```
```cpp
std::string extension() const
```

- メンバ変数 : ``m_`` + ``camelCase``(構造体の公開された変数などは、例外的に``m_``をつけない)
- 静的メンバ変数 : ``s_`` + ``PascalCase``

```cpp
std::string m_path;
```

```cpp
static AssetMap s_Assets;
```

- ローカル変数 : ``camelCase``
```cpp
std::string ext;
std::string filePath;
```

- ``定数``/``enum値`` : ``PascalCase``
	- ``グローバル定数``は``k``プレフィックスを付けてもよい
```cpp
constexpr int kMaxLightCount = 8;

enum class AssetType : u8 {
    None = 0,    
    Texture,     
    Shader,      
    Mesh,        
    Material,    
    Audio,       
};
```

- 名前空間 : ``PascalCase``
```cpp
namespace Tsukino::Asset{ ... }
```

### 4-3. ファイル名の命名規則
- 基本的に**PascalCase**を使用する
    - 例: `TextureLoader.h`, `AssetManager.cpp`など

- クラスを定義するヘッダは、クラス名と同じファイル名にする  
    - 例: `AssetManager.h` に `class AssetManager` を定義する

- **例外**
    - 慣習的に小文字で書かれることが多いユーティリティや、プリコンパイルヘッダーなど**lower-case**を許容する
    - 例 : `pch.h`, `typedef.h`など

# 5. クラス設計と責務
- クラスは**単一責務**を基本とする
- 依存関係は**一方向**になるように設計する
- コンストラクタでは**重い処理**（**ファイルIO**, **GPUリソース確保など**）を避ける
    - 必要なら``initialize()``など明示的な初期化関数を用意する
- ``RAII``を徹底し、**リソース解放漏れ**を防ぐ

### 5-1. メンバ宣言順は破棄順序の設計である

- メンバは**宣言の逆順に破棄される**。つまり**上に書いたものほど長生きする**
- 相互に参照するメンバを持つクラスでは、**依存される側を先に宣言する**
- 順序に意味がある場合は、**なぜその順序なのかをコメントで残す**

```cpp
class Scene {
private:
    //! @note 【この宣言順は破棄順序の設計であり、並べ替えてはならない】
    //! System は ScopedConnection 経由で EventBus へ Unsubscribe を投げるため、
    //! EventBus は System より長生きしなければならない。
    EventBus      m_eventBus;         // 最も長生きさせる
    Registry      m_registry;
    SystemManager m_systemManager;    // 最初に破棄する
};
```

- 「たまたま落ちていない」状態になりやすく、シーン切替やアプリ終了という
  **テストで踏みにくい経路**で初めて表面化するため、特に注意する

### 5-2. ECS の外に実体があるリソースの回収

- ``GPUリソース``, ``物理ボディ``, ``エフェクトハンドル``のように
  **所有権が ECS の外にあるもの**は、``イベントバス``ではなく
  ``Registry::OnDestroy<T>()``（``EnTT``の破棄シグナル）で回収する
    - イベントバスは``Scene::DestroyEntity()``を通った場合しか発火しないが、
      破棄シグナルは**どの破棄経路でも必ず発火する**ため
- **購読したら必ずデストラクタで解除する**
    - ``System``は``Registry``より先に破棄されるため、解除しないと
      破棄済みの``this``へコールバックが飛ぶ

### 5-3. System からのエンティティ破棄

- ``System``の中からエンティティを破棄する場合は
  必ず``Registry::QueueDestroy()``を使う
- ``Registry::DestroyEntity()``は**即時破棄**であり、
  ``View``の反復中に呼ぶと``EnTT``のイテレータが壊れる
- 実際の破棄は``Scene::Update()``が全``System``の更新後にまとめて行う

### 5-4. hlsl++ の合成順は行列とクォータニオンで逆

``hlslpp::mul()``は引数の型によって合成順の意味が変わる。
「**Aを適用してからBを適用する**」を書くときは:

| 型 | 書き方 |
| --- | --- |
| 行列（行ベクトル規約） | ``mul(A, B)`` |
| クォータニオン（Hamilton積） | ``mul(B, A)`` ← **後から効かせる側を左** |

- 根拠は``hlslpp``本体の``External/hlslpp/unit_tests/unit_tests_quaternion.cpp``
  （``M_AB = M_A * M_B`` / ``Q_AB = Q_B * Q_A``）
- ローカル姿勢をワールドへ持ち上げる合成は``mul(parentRotation, localRotation)``と書く
- 行列と同じ順で書くと、回転軸が親の向きの逆回転で回ってしまい
  「**キャラクターの向きによって挙動が変わる**」類の不具合になる

ベクトルの回転は**オーバーロードごとに意味が違う**ので混同しない。

```cpp
hlslpp::mul(v, q);   // 前方回転（q v q*）。ローカル→ワールドはこちら
hlslpp::mul(q, v);   // 逆回転（_hlslpp_quat_mul_vec_inv_ps）
```

# 6. エラーハンドリング

- ログメッセージは英語で、原因と影響が分かるように書く
```cpp
MessageBoxA(nullptr, "Failed to load shader.", "Error", MB_OK);
```

# 7. アセットとパスの扱い
- コード中に``絶対パス``を書かない
- アセットは**プロジェクトルート配下**の``Assets/``に配置する
- 実行時は``bin/<Config>/``からの``相対パス``でアクセスすることを前提とする。
```cpp
Tsukino::Core::Path         path("Assets/Shaders/test.hlsl");
```
- ビルドスクリプト(``Premake``)で
    - ``Assets/``を``bin/<Config>/``にコピーするルールを定義する
- パスには``Core``の``Path``クラスを使用し、``OS``依存の記述を避ける
- **一時ログ・調査用の出力は``Logs/``配下に出す。** デバッグ用の``std::ofstream``を
  カレントディレクトリ直下に開かないこと。リポジトリを汚し、``git diff``や``git log -p``を
  膨らませる（``Logs/``は``.gitignore``済み）
- 実行結果をファイルで確認したいときは``Log::SetLogFile("Logs/Tsukino.log")``を呼ぶ。
  呼ばない限り``Log``は``OutputDebugStringA``にしか出ない

# 8. ドキュメントとコメント
- **なぜそうしているか**をコメントに書く
    - 何をしているかはコードから読み取れるべき
- 公開APIには``Doxygen``形式のコメントを推奨する
```cpp
    //--------------------------------------------------------------
    //! @brief  パス文字列を取得する関数
    //! @return パス文字列
    //--------------------------------------------------------------
    const std::string& string() const noexcept { return m_path; }
```

# 9. Git運用とコミットメッセージ
- コミットは**論理的な変更単位**で小さくまとめる
- コミットメッセージは英語で、``[]``内に内容のカテゴリを記述する
```Text
[Implement] AssetManager::Load()
[Add] shader loading support to AssetManager
```

### コミットカテゴリ一覧
- ``[Add]`` : 新機能の追加
- ``[Fix]`` : バグ修正
- ``[Update]`` : 挙動変更や更新
- ``[Refactor]`` : コードのリファクタリング（機能変更なし）
- ``[Docs]`` : ドキュメントの追加・修正
- ``[Remove]`` : 削除

- ``自動生成ファイル``や``ビルド成果物``はコミットしない（``.gitignore``で除外）

# 10.規約の更新
- このファイルは**固定ではなく、プロジェクトの成長に合わせて更新する**
- 変更する場合は既存コードとの整合性を確認する