必ず日本語で応答してください。

> **このファイルは GitHub Copilot 向けの写しです。規約の正は `CODING_GUIDELINES.md`。**
> 食い違いがあれば `CODING_GUIDELINES.md` を優先し、直すときはそちらを先に直すこと。
> コメントの書式は `.claude/skills/tsukino-doc-comment/SKILL.md` が正。

# コード規約

- ファイル名はすべてPascal Case
- 文字コードは UTF8 crlf
- stdはusingしません。
- C++23までの記述
- 簡潔に記述できる場合はなるべくSTLを活用
- 可読性も重要視します、実行速度が犠牲にならないよう留意します。
- デフォルト値初期化には = {}; を使わず {}; を使用してください。
- 変数宣言は1行に1つを宣言。カンマで複数定義しない。

- 変数名や関数名などのシンボルは基本的には省略形を使用しません。
    - 極端に長くなりすぎないように適切な意味の単語を使用
    - 但し、10行以内のスコープ内のローカル変数は省略系を利用しても構いません。
    - ループカウンターは省略しても構いません(i,j,kなど)。但し、範囲for文の変数は省略しないでください。

### 共通方針
- 英語で命名する
- 略語は一般的なもの（``URL``, ``ID``, ``CPU``等）のみに限定する
- 名前から役割とスコープが推測できること

### C++の命名

- **クラス**/**構造体** : ``PascalCase``
```cpp
class TextureLoader;
struct VertexBufferDesc;
```

- **関数**/**メソッド** : ``PascalCase ``
	- ただし**STL互換の関数**や、**ファイル名取得などの軽量アクセサ**は``camelCase``を許容
```cpp
int GetWidth();
```
```cpp
std::string extension() const
```

- メンバ変数 : ``m_`` + ``camelCase``(構造体の公開された変数などは、例外的に``m_``をつけない)
- 静的メンバ変数 : ``s_`` + ``PascalCase``

```cpp
std::string m_path;
```

```cpp
static AssetMap s_Assets;
```

- ローカル変数 : ``camelCase``
```cpp
std::string ext;
std::string filePath;
```

- ``定数``/``enum値`` : ``PascalCase``
	- ``グローバル定数``は``k``プレフィックスを付けてもよい
```cpp
constexpr int kMaxLightCount = 8;

enum class AssetType : u8 {
    None = 0,    
    Texture,     
    Shader,      
    Mesh,        
    Material,    
    Audio,       
};
```

- 名前空間 : ``PascalCase``
```cpp
namespace Tsukino::Asset{ ... }
```

### ファイル名の命名規則
- 基本的に**PascalCase**を使用する
    - 例: `TextureLoader.h`, `AssetManager.cpp`など

- クラスを定義するヘッダは、クラス名と同じファイル名にする  
    - 例: `AssetManager.h` に `class AssetManager` を定義する

- **例外**
    - 慣習的に小文字で書かれることが多いユーティリティや、プリコンパイルヘッダーなど**lower-case**を許容する
    - 例 : `pch.h`, `typedef.h`など

# クラス設計と責務
- クラスは**単一責務**を基本とする
- 依存関係は**一方向**になるように設計する
- コンストラクタでは**重い処理**（**ファイルIO**, **GPUリソース確保など**）を避ける
    - 必要なら``initialize()``など明示的な初期化関数を用意する
- ``RAII``を徹底し、**リソース解放漏れ**を防ぐ

# エラーハンドリング

- ログメッセージは英語で、原因と影響が分かるように書く
```cpp
MessageBoxA(nullptr, "Failed to load shader.", "Error", MB_OK);
```


# 実装の方針

## Vulkan API の特定の関数の引数に関する厳格なルール

- `vkGetPhysicalDeviceSurfaceFormatsKHR` と `vkGetPhysicalDeviceSurfacePresentModesKHR` を呼び出す際は、必ず4つの引数を渡してください。これらの関数は頻繁に引数の数で間違いが発生しています。

- **`vkGetPhysicalDeviceSurfaceFormatsKHR` の正しい呼び出し方:**
  - 1番目の引数: `VkPhysicalDevice`
  - 2番目の引数: `VkSurfaceKHR`
  - 3番目の引数: `uint32_t*` (カウント用ポインタ)
  - 4番目の引数: `VkSurfaceFormatKHR*` (データ用ポインタ、または `nullptr`)
  - **例:** `vkGetPhysicalDeviceSurfaceFormatsKHR(physicalDevice, surface, &formatCount, formats.data());`

- **`vkGetPhysicalDeviceSurfacePresentModesKHR` の正しい呼び出し方:**
  - 1番目の引数: `VkPhysicalDevice`
  - 2番目の引数: `VkSurfaceKHR`
  - 3番目の引数: `uint32_t*` (カウント用ポインタ)
  - 4番目の引数: `VkPresentModeKHR*` (データ用ポインタ、または `nullptr`)
  - **例:** `vkGetPhysicalDeviceSurfacePresentModesKHR(physicalDevice, surface, &presentModeCount, presentModes.data());`

- **特に、2番目の引数である `VkSurfaceKHR` を省略しないように、常に注意を払ってください。**

# シェーダーファイル名の規約

- ファイル名はすべて小文字の Snake case
- シェーダーの種類によって先頭に接頭辞を追加 (vs_, ps_, gs_, cs_, ms_, as_ など)
- 拡張子は fx
- エントリーポイント関数名は main
- シェーダーファイルを新規追加する場合はプロジェクトに追加しないようにしてください。
    - 又はビルドから除外する設定にする場合は追加しても構いません

# コメント文の規約

コメント文は日本語で記述してください。稀に別の言語が混入するようですので厳密に日本語で記述してください。
初学者にもわかるように丁寧に各処理にコメント記述してください。

- コメント文は // で記述し、//の直後はスペースを空ける。
    【例】// コメント文
- **コメントの文体:**
    - **原則（体言止め）:** 関数要約以外の、処理内容などを説明する通常のコメントは、簡潔さを重視し「～の処理」「～を設定」のように**体言止め**で記述します。体言止めの場合、文末に句点「。」は付けません。
    - **例外（です・ます調）:** **関数の要約コメント**（`@brief`に相当する部分）は、Doxygenで生成されるドキュメントの解説文として読まれることを想定し、丁寧な**です・ます調の文章**で記述します。
        - **ただし、「この関数は～」という主語は冗長になるため必ず省略し、「（目的語）を～します。」のように、目的語または動詞から文章を始めてください。**

- **書式例（ヘッダーファイル）:**
    ```cpp
    //【良い例】: 主語を省略し、動詞から始めている
    //  バッファを作成します。
    //! @param [in] desc バッファデスクリプタ。
    //! @return 作成されたバッファリソース。
    std::unique_ptr<Resource> CreateBuffer(const BufferDesc& desc);
    ```
    ```cpp
    //【悪い例】: 「この関数は」という冗長な主語が含まれている
    //  この関数はバッファを作成します。
    //! @param [in] desc バッファデスクリプタ。
    //! @return 作成されたバッファリソース。
    std::unique_ptr<Resource> CreateBuffer(const BufferDesc& desc);
    ```
    
- @paramの引数解説は 「@param [in,out] value 解説」 という書式にします。
    - [in,out] の箇所は入力の場合は [in] 値を受け取って出力のみの場合は [out]、値を渡しながら受け取る場合は [in,out] と記述します。
- @のあとの記述は空白区切りの位置で前後のコメントと位置をスペースで揃えます
    - 【例】
        //! @param  [in] value         変数コメン名
        //! @param  [in] longValueName 長いコメント
        //! @return 戻り値
         
- 変数宣言の説明は右側に記述
    【例】int value;    // コメント文

- doxygen記法でドキュメント生成に対応させます
    - コメントは `//!` または `//!<` で記述します。

    - **`@brief` タグの使用に関する厳格なルール:**
        - `@brief` タグは、**ファイル先頭のファイルコメントブロック内でのみ、ただ一度だけ使用が許可されます。**
        - それ以外のすべての場所（関数、クラス、構造体の要約など）で `@brief` タグを**使用することは固く禁止します。**
        - 要約は、常に `//!` の直後に記述してください。
        - **正しい例（関数要約）:** `//! これは要約です。`
        - **禁止例（関数要約）:** `//! @brief これは要約です。`

    - **【重要】関数コメントの記述ルール:**
        - 各関数のコメントブロックの直前には、必ず1行の空行を挿入してください。

        - **ヘッダーファイル (.h, .hpp) 側:**
            - **要約、引数、戻り値**のすべてを記述します。
            - **要約:** `//!` の直後に記述します。**`@brief` は絶対に使用しないでください。**
            - **引数:** `//! @param [in/out] name 説明` の形式で記述します。
            - **戻り値:** `//! @return 説明` の形式で記述します。（戻り値がない場合は不要）
            - **書式例:**
              ```cpp
              //! バッファを作成します。
              //! @param [in] desc バッファデスクリプタ。
              //! @return 作成されたバッファリソース。
              std::unique_ptr<Resource> CreateBuffer(const BufferDesc& desc);
              ```

        - **ソースファイル (.cpp) 側:**
            - **要約のみ**を記述します。引数（`@param`）や戻り値（`@return`）の記述は**禁止**です。
            - コメントの前後の行に、76文字のハイフン `-` で構成される罫線を入れます。
            - **書式例:**
              ```cpp
              //----------------------------------------------------------------------------
              //! バッファを作成します。
              //----------------------------------------------------------------------------
              std::unique_ptr<iRHI::Resource> VulkanDevice::CreateBuffer(const iRHI::BufferDesc& desc)
              {
                  // ...実装...
              }
              ```

- **【最重要】ヘッダーとソースでの関数コメントの役割分担:**
    - **AIの思考プロセス:** 関数実装（.cpp）のコメントを生成する際は、以下の手順を厳密に守ってください。
        1.  まず、その関数の**宣言がヘッダーファイル (.h, .hpp) に存在するかどうかを必ず確認します。**
        2.  確認結果に応じて、下記のいずれかのルールを適用します。

    - **ルールA: 関数宣言がヘッダーに【存在する】場合（`public`, `protected`, `private` すべてのメンバー関数）:**
        - **このルールは、クラスのアクセス指定子（`public`, `protected`, `private`）に関わらず、ヘッダーファイルで宣言されているすべてのメンバー関数に適用されます。**
        - ソースファイル（`.cpp`）側には、ハイフン罫線で囲んだ**要約コメントのみ**を記述します。
        - `@param` や `@return` などの詳細なDoxygenタグは、ヘッダー側に集約されているため、ソースファイル側には**絶対に記述しないでください。**
        - **書式例 (.cpp):**
          ```cpp
          //----------------------------------------------------------------------------
          //! バッファを作成します。
          //----------------------------------------------------------------------------
          std::unique_ptr<iRHI::Resource> VulkanDevice::CreateBuffer(const iRHI::BufferDesc& desc)
          {
              // ...実装...
          }
          ```

    - **ルールB: 関数宣言がヘッダーに【存在しない】場合（例: `static`関数、ラムダなど）:**
        - ソースファイル（`.cpp`）が唯一のドキュメント場所となるため、**すべての解説（要約, `@param`, `@return`）をソース側に記述します。**
        - これは、ヘッダーファイルにコメントを記述する場合のルールに準じます。
        - **書式例 (.cpp 内の static 関数):**
          ```cpp
          //----------------------------------------------------------------------------
          //! Vulkan バリデーションレイヤーのデバッグコールバック関数です。
          //! @param  [in] messageSeverity メッセージの重要度
          //! @param  [in] messageType     メッセージの種類
          //! @param  [in] pCallbackData   コールバックデータ
          //! @param  [in] pUserData       ユーザーデータ
          //! @return 常に VK_FALSE を返し、実行を継続します。
          //----------------------------------------------------------------------------
          static VKAPI_ATTR VkBool32 VKAPI_CALL DebugCallback(...)
          {
              // ...実装...
          }
          ```

- ファイルの先頭部分にはファイルコメントを作成します。
    - コメントブロックは、76文字のハイフン (-) で構成される罫線で囲みます。
    - ブロック内には以下のDoxygenタグを順番に記述します。
        1.  `@file`: ファイル名を記述します。
        2.  `@brief`: ファイルの概要を**体言止め**で簡潔に記述します。（例: `Vulkan RHI の実装`）
        3.  `@detail`: ファイル内の実装が持つ役割や処理内容の詳細を、**です・ます調の文章**で詳しく解説します。
    - `@brief` と `@detail` の文章がほぼ似てしまう場合は `@detail` を省略しても構いません。
    - このファイルコメントブロックは、ファイルの最も先頭に配置します。
    - コメントブロックの直後には**空行を入れず**、次のコード（例: `#include`）を記述します。このルールは厳密に守ってください。
    - **書式例:**
        ```cpp
        //----------------------------------------------------------------------------
        //! @file   iRHI_vulkan.cpp
        //! @brief  Vulkan RHI の実装
        //! @detail Vulkan API を利用して iRHI インターフェースを実装します。
        //!         ウィンドウの作成から、デバイスの選択、スワップチェーンの管理、
        //!         コマンドの実行まで、グラフィックスレンダリングに必要な
        //!         一連の機能を提供します。
        //----------------------------------------------------------------------------
        #include "header.h"
        ```
        
    - 各クラスの先頭には、Doxygen用のコメントブロックを記述します。
      - コメントの前後の行を `=` 記号を76文字使った罫線で囲みます。（この文字数は厳守してください）
      - 1行目に `//! @class [クラス名]` を記述します。
      - **ヘッダーファイル (.h, .hpp) の場合:**
        - 2行目に `//!` に続けてクラスの**要約**を記述します。
        - **要約は「このクラスは」という主語を省略し、「〇〇を行うためのクラスです。」のように記述します。**
        - **書式例 (.h):**
          ```cpp
          //============================================================================
          //! @class  MyClass
          //! 〇〇を行うためのクラスです。
          //============================================================================
          class MyClass { ... };
          ```
      - **ソースファイル (.cpp) の場合:**
        - 2行目に `//! @detail` に続けて、実装が持つ役割や処理内容の**詳細**を記述します。
        - **詳細**は「このクラスは」という主語を省略して記述します。
        - **書式例 (.cpp):**
          ```cpp
          //============================================================================
          //! @class  MyClass
          //! @detail 〇〇の機能を実現するための具体的な実装を提供します。
          //!         内部状態の管理や、複雑なアルゴリズムの実行を担当します。
          //============================================================================
          ```
    
    - 各構造体の先頭には、Doxygen用のコメントブロックを記述します。
      - コメントの前後の行を `-` 記号を76文字使った罫線で囲みます。（この文字数は厳守してください）
      - 1行目に `//! @struct [構造体名]` を記述します。
      - **ヘッダーファイル (.h, .hpp) の場合:**
        - 2行目に `//!` に続けて構造体の**要約**を記述します。
        - **要約は「この構造体は」という主語を省略し、「〇〇のデータを保持する構造体です。」のように記述します。**
        - **書式例 (.h):**
          ```cpp
          //--------------------------------------------------------
          //! @struct MyStruct
          //! 〇〇のデータを保持する構造体です。
          //--------------------------------------------------------
          struct MyStruct { ... };
          ```
      - **ソースファイル (.cpp) で定義する場合 (例: PImplイディオム):**
        - 2行目に `//! @detail` に続けて、構造体が持つ役割やデータの**詳細**を記述します。
        - **詳細**は「この構造体は」という主語を省略して記述します。
        - **書式例 (.cpp):**
          ```cpp
          //--------------------------------------------------------
          //! @struct MyStruct::Impl
          //! @detail MyClass のプライベートな実装を隠蔽するためのデータ構造です。
          //!         ウィンドウハンドルや内部リソースへのポインタを保持します。
          //--------------------------------------------------------
          struct MyStruct::Impl { ... };
          ```
    
    - メンバー変数は //!< でインラインコメントで各変数を丁寧に解説します。

- **コンストラクタ初期化子リストのコメント:**
    - コンストラクタの初期化子リストでメンバー変数を初期化する場合、各行の右側にその初期化の意図を説明するコメントを記述します。
    - コメントには `//` を使用し、`//!<` は**使用しません**。これは、メンバー変数の宣言（ヘッダーファイル内）で `//!<` を使用するため、二重定義を避けるためです。
    - コメントの開始位置は、その初期化子リスト内で最も長い行の末尾から**半角スペース4つ分**を空けた位置を基準に、縦に揃えます。
    - **書式例:**
      ```cpp
      MyClass::MyClass(int value, bool flag)
          : m_value(value)      // 値を初期化
          , m_flag(flag)        // フラグを設定
          , m_pointer(nullptr)  // ポインタはヌルで初期化
      {
          // ...
      }
      ```

- 関数内はアウトラインのように階層構造のように関数内に完結するように記述します。書式は下記のとおりです
    - 大見出し。全体的な流れを知るためにこのコメント文だけを読めばわかるように要約します。
      前と後の行にハイフンを56文字で記述。（この文字数は厳守し、エディタの幅などに影響されないようにしてください）




        //--------------------------------------------------------
        // コメント文
        //--------------------------------------------------------

    - 中見出し。大見出しの中の手順を記述するために利用します。
      - コメント文の上にのみ、ハイフン `-` で構成される罫線を記述します。コメント文の下には罫線を記述しません。
      - 罫線の長さは、すぐ下のコメント文の表示幅に合わせて動的に決定します。
      - **罫線の長さの計算方法:**
        1. コメント文の本体部分（`//` とその直後の半角スペースを除いたテキスト）の表示幅を計算します。
           - **半角文字**（英数字、記号、スペースなど）: 1文字あたり **1** としてカウントします。
           - **全角文字**（日本語など）: 1文字あたり **2** としてカウントします。
        2. 罫線の長さは、以下の式で算出します。
           `(算出した表示幅) + 3`
        3. ただし、計算結果が48文字を超える場合、罫線の長さは**48文字**を上限とします。
      - **書式例:**
        ```cpp
        //--------------------------------  <-- 計算: (全角14文字 * 2) + 3 = 31文字
        // サーフェスフォーマットの選択

        //------------------------------------  <-- 計算: (半角33文字 * 1) + 3 = 36文字
        // This is a medium heading comment.
        ```        
    - 通常見出し。そのまま記述
        // コメント文
    
    - 見出しの直後の行に、コメント文以外が記述される場合は実装との間の空行を除去します
      【例】
          //--------------------------------------------------------
          // コメント文
          //--------------------------------------------------------
          for(int i=0; ....

    - 大見出しと中見出しは連続しすぎないように、通常見出しと併用して可読性を向上させます

- 数値の前後の空白は日本語として不自然に見えることがあります。空白を入れないようにしてください。
- 読点の挿入は前後が両方ひらがなかカタカナの場合のみ。

 # 【厳格ルール】コメント内の手順リストの禁止
 
 複数のコメント行にまたがって、手順を説明するために連番（例: `1.`, `2.`, `3.` や `①`, `②`, `③`）を使用することは固く禁止します。処理の流れは、見出しや文章で説明してください。
 
 **【悪い例】: 禁止する連番コメント**
 ```cpp
 // 1. 最初にインスタンスを作成
 VkInstanceCreateInfo createInfo{};
 
 // 2. 次にデバッグメッセンジャーを設定
 VkDebugUtilsMessengerCreateInfoEXT debugCreateInfo{};
 
 // 3. 最後に物理デバイスを選択
 uint32_t deviceCount = 0;
 ```
 
 **【良い例】: 文章や見出しで説明する**
 ```cpp
 //------------------------------------
 // Vulkan インスタンスの初期化
 
 // インスタンスの作成
 VkInstanceCreateInfo createInfo{};
 
 // デバッグメッセンジャーの設定
 VkDebugUtilsMessengerCreateInfoEXT debugCreateInfo{};
 
 //------------------------------------
 // 物理デバイスの選択
 
 // 利用可能な物理デバイスの数を取得
 uint32_t deviceCount = 0;
 ```
 
 **【例外】: 1つのコメント内での手順説明**
 
 手順の説明がどうしても必要な場合は、以下のように**1つのコメントブロック内に限り**記述を許可します。
 
 ```cpp
 // この関数は、以下の手順で処理を実行します: (1)リソースを確保し、(2)パラメータを検証し、(3)コマンドをキューに投入します。
 ```

 # 【最重要・厳格ルール】構造体メンバーへの連続代入コメント
 
 これはコード全体の可読性に関わる最重要ルールです。**いかなる場合でも厳密に遵守してください。**
 
 ## 1. ルールの概要
 
 **2行以上**にわたって、**同一の構造体変数**のメンバーに値を連続して設定するすべてのコードブロックに、**整列された行コメント**を付与します。
 
 ## 2. 適用対象 (具体的なコードパターン)
 
 以下のパターンに完全に一致するコードブロックが対象です。
 
 - **パターンA: 変数宣言後の連続代入**
 ```
   // このパターンが対象
   VkSomeStruct myStruct{};
   myStruct.memberA = valueA; // 2行以上の連続代入
   myStruct.memberB = valueB;
   myStruct.memberC = valueC;
 ```
 
 - **パターンB: コンストラクタ初期化子リスト**
 ```
   // このパターンも対象
   MyClass::MyClass()
       : memberA(valueA) // 2行以上の連続初期化
       , memberB(valueB)
   {
       // ...
   }
 ```
 
 ## 3. 必須要件 (チェックリスト)
 
 対象ブロックを見つけたら、以下の3つの要件を**必ず**満たしてください。
 
 1.  **【全行コメント】**: 値を設定している**すべての行**の右側に、そのメンバーの目的を説明するコメントを記述します。
 2.  **【`//` を使用】**: コメント記法は `//` を使用します。(`//!<` は禁止)
 3.  **【厳格な整列】**: ブロック内の**すべての `//` の開始位置を、縦に完全に揃えます。**
 
 ## 4. 整列（アライメント）の具体的な手順
 
 1.  まず、コメントを付けたい連続代入のブロック（例：`myStruct.memberA = ...;` から `myStruct.memberC = ...;` まで）の中から、**最も長い行**を見つけ出します。
 2.  その最も長い行の末尾から、**半角スペースを4つ**空けます。
 3.  その位置が、そのブロック全体の**すべてのコメントを開始すべき縦のライン（カラム）**となります。
 4.  他の短い行にも、この縦のラインに `//` が揃うように、必要な数の半角スペースを挿入します。
 
 ## 5. 書式例 (この通りに実装してください)
 
 **例1: `VkDescriptorBufferInfo`**
 ```
 // 【正しい実装】
 VkDescriptorBufferInfo bufferInfo{};
 bufferInfo.buffer = vkBuffer->GetVkBuffer();      // バッファハンドル
 bufferInfo.offset = 0;                            // バッファの先頭からのオフセット
 bufferInfo.range  = vkBuffer->GetBufferDesc().Size; // ディスクリプタがカバーするサイズ
 ```
 
 **例2: `VkWriteDescriptorSet`**
 ```
 // 【正しい実装】
 VkWriteDescriptorSet descriptorWrite{};
 descriptorWrite.sType           = VK_STRUCTURE_TYPE_WRITE_DESCRIPTOR_SET; // 構造体の種類
 descriptorWrite.dstSet          = descriptorSet;                          // 更新先のディスクリプタセット
 descriptorWrite.dstBinding      = 0;                                      // レイアウト内のバインディング番号
 descriptorWrite.dstArrayElement = 0;                                      // 配列の場合の開始要素インデックス
 descriptorWrite.descriptorType  = VK_DESCRIPTOR_TYPE_UNIFORM_BUFFER;      // ディスクリプタの種類 (UBO)
 descriptorWrite.descriptorCount = 1;                                      // 更新するディスクリプタの数
 descriptorWrite.pBufferInfo     = &bufferInfo;                            // バッファ情報へのポインタ
 ```
