//--------------------------------------------------------------
//! @file   Tonemap.ps.hlsl
//! @brief  トーンマッピング用ピクセルシェーダー
//! @author 山﨑愛
//--------------------------------------------------------------

//--------------------------------------------------------------
//! @brief HDRレンダーターゲット (t0)
//--------------------------------------------------------------
Texture2D hdrTexture : register(t0);
SamplerState hdrSampler : register(s0);

struct PSInput
{
    float4 position : SV_POSITION;
    float2 uv : TEXCOORD0;
};

//--------------------------------------------------------------
// 露出倍率。ACESへ入れる前にHDR値へ掛ける。
//
// HDRバッファの値は物理的な輝度ではなく、lightColor.w（ゲーム側が5.0）や
// SkyAtmosphereComponent::sunIntensity（同20.0）といった任意スケールの
// 積み重ねでできている。そのままACESへ入れるとニー（1.0付近）を大きく超えて
// 白へ張り付くので、ここで一度落としてからカーブに乗せる。
//
// 0.6という値に物理的な根拠は無く、既存の絵から引き継いだ経験値。
// 全体の明るさを変えたいときは、ここではなく光源側
// （DirectionalLightComponent::intensity）を動かすほうが筋が良い。
// こちらは空・エミッシブ・パーティクル・フォグまで一律に動かしてしまう
//--------------------------------------------------------------
static const float kExposure = 0.6f;

//--------------------------------------------------------------
//! @brief ACESトーンマッピング
//--------------------------------------------------------------
float3 ACES(float3 x)
{
    float a = 2.51f;
    float b = 0.03f;
    float c = 2.43f;
    float d = 0.59f;
    float e = 0.14f;
    return saturate((x * (a * x + b)) / (x * (c * x + d) + e));
}

//--------------------------------------------------------------
//! @brief ピクセルシェーダーメイン
//--------------------------------------------------------------
float4 PSMain(PSInput input) : SV_TARGET
{
        // UV のY を反転
    float2 uv = float2(input.uv.x, 1.0f - input.uv.y);

    // HDRバッファをサンプリング
    float4 hdrColor = hdrTexture.Sample(hdrSampler, uv);

    // ACESトーンマッピング（RGBのみ）。この1回がパイプライン唯一のトーンマップで、
    // アルベドやBRDFの途中結果には掛けない
    float3 ldrColor = ACES(hdrColor.rgb * kExposure);

    // アルファは乗算せずそのまま返す。
    // このパスはOpaqueブレンドでバックバッファへ描かれるため出力アルファに消費者がおらず、
    // 以前ここで行っていた事前乗算（RGBにアルファを乗算）は「HDRアルファが1未満の画素を
    // 暗くする」副作用しか持たなかった。不透明ジオメトリのアルファがHDRバッファへ漏れると
    // 透明テクセルが真っ黒に潰れる不具合の原因になっていたため取り除いている
    return float4(ldrColor, 1.0f);
}
